题目给了一个doll.bin文件，用file命令查看发现是gzip压缩文件，尝试解压缩，发现解出来的还是gzip压缩文件。于是猜测是多次压缩后的文件。所以写了一个python程序自动解压缩。大概解压了997次之后得到了不是压缩文件的文件。文件里就是答案babouchka。
PS：这个python程序在Ubuntu下能正常运行，但是在Windows下运行到第二遍就报CRC校验错误，不知是什么原因。